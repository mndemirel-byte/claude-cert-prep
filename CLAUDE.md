## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `mndemirel-byte/claude-cert-prep`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at the repo root, ADRs under `docs/adr/`. See `docs/agents/domain.md`.
